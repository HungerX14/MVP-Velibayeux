{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p var && php bin/console cache:warmup && php bin/console doctrine:migrations:migrate --no-interaction && php bin/console doctrine:fixtures:load --no-interaction && php -S 0.0.0.0:$PORT -t public public/index.php",
    "healthcheckPath": "/api/stations",
    "restartPolicyType": "ON_FAILURE"
  }
}
