{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p var/cache var/log && php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration 2>/dev/null || true && php bin/console doctrine:fixtures:load --no-interaction 2>/dev/null || true && php -S 0.0.0.0:$PORT -t public public/index.php",
    "healthcheckPath": "/api/stations",
    "restartPolicyType": "ON_FAILURE"
  }
}
